<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="losertron4000.MainPage">

    <Grid RowSpacing="0" ColumnSpacing="0">
        <!-- We define two rows of equal height using '*' for each row -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top half: Image -->
        <Image x:Name="dokiPreview" Grid.Row="0"
               Source="sample.png"
               Aspect="AspectFit"
               HorizontalOptions="FillAndExpand"
               VerticalOptions="FillAndExpand" />

        <!-- Bottom half: ScrollView containing a 3xY Grid -->
        <ScrollView Grid.Row="1"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="FillAndExpand">
            <Grid>
                <!-- Define three columns -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label x:Name="gogo" Grid.Row="0" Grid.Column="0" Text="Item (0,0)" />
                <Label Grid.Row="0" Grid.Column="1" Text="Item (0,1)" />
                <Label Grid.Row="0" Grid.Column="2" Text="Item (0,2)" />

                <Label Grid.Row="1" Grid.Column="0" Text="Item (1,0)" />
                <Label Grid.Row="1" Grid.Column="1" Text="Item (1,1)" />
                <Label Grid.Row="1" Grid.Column="2" Text="Item (1,2)" />

            </Grid>
        </ScrollView>
    </Grid>
</ContentPage>
